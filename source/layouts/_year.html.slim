.card
  .card-header
    | Archives
  .list-group
    - blog.articles.group_by {|a| a.date.year }.each do |year, articles|
      = link_to blog_year_path(year), class: 'list-group-item full'
        .full
          span.pull-left
            | #{year}
            |  (#{articles.size})
          span.pull-right
            i.fa.fa-arrow-right aria-hidden="true"
